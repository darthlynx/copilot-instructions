# Home Overlay

For personal labs and experiments:
- still keep the design v2-native
- prefer namespaced resources first
- avoid claims even in small demos unless explicitly testing migration paths
- use simpler examples, but keep scope and provider config references explicit
