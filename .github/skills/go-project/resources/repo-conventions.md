# Repository Conventions

Follow repository conventions before applying generic Go preferences.

Check:
- package naming patterns
- error handling style
- logging library
- test style
- layout such as `cmd/`, `internal/`, `pkg/`
- whether the repo uses `make`, plain `go` commands, task runners, or scripts

Prefer consistency with the existing codebase unless it is clearly harmful.
